apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app850-svc
  namespace: clawtest
  labels:
    app: perf-test5-app850
spec:
  selector:
    app: perf-test5-app850
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
