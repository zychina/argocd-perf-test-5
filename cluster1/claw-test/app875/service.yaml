apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app875-svc
  namespace: clawtest
  labels:
    app: perf-test5-app875
spec:
  selector:
    app: perf-test5-app875
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
