apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app398-svc
  namespace: clawtest
  labels:
    app: perf-test5-app398
spec:
  selector:
    app: perf-test5-app398
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
