apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app906-svc
  namespace: clawtest
  labels:
    app: perf-test5-app906
spec:
  selector:
    app: perf-test5-app906
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
