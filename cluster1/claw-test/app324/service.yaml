apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app324-svc
  namespace: clawtest
  labels:
    app: perf-test5-app324
spec:
  selector:
    app: perf-test5-app324
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
