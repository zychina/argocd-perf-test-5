apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app505-svc
  namespace: clawtest
  labels:
    app: perf-test5-app505
spec:
  selector:
    app: perf-test5-app505
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
