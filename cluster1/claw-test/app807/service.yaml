apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app807-svc
  namespace: clawtest
  labels:
    app: perf-test5-app807
spec:
  selector:
    app: perf-test5-app807
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
