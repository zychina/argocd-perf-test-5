apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app535-svc
  namespace: clawtest
  labels:
    app: perf-test5-app535
spec:
  selector:
    app: perf-test5-app535
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
