apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app869-svc
  namespace: clawtest
  labels:
    app: perf-test5-app869
spec:
  selector:
    app: perf-test5-app869
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
