apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app262-svc
  namespace: clawtest
  labels:
    app: perf-test5-app262
spec:
  selector:
    app: perf-test5-app262
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
