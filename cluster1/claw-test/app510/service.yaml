apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app510-svc
  namespace: clawtest
  labels:
    app: perf-test5-app510
spec:
  selector:
    app: perf-test5-app510
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
