apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app41-svc
  namespace: clawtest
  labels:
    app: perf-test5-app41
spec:
  selector:
    app: perf-test5-app41
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
