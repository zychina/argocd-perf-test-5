apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app271-svc
  namespace: clawtest
  labels:
    app: perf-test5-app271
spec:
  selector:
    app: perf-test5-app271
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
