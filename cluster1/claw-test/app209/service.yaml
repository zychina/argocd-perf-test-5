apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app209-svc
  namespace: clawtest
  labels:
    app: perf-test5-app209
spec:
  selector:
    app: perf-test5-app209
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
