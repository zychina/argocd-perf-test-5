apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app483-svc
  namespace: clawtest
  labels:
    app: perf-test5-app483
spec:
  selector:
    app: perf-test5-app483
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
