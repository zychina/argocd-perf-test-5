apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app204-svc
  namespace: clawtest
  labels:
    app: perf-test5-app204
spec:
  selector:
    app: perf-test5-app204
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
