apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app755-svc
  namespace: clawtest
  labels:
    app: perf-test5-app755
spec:
  selector:
    app: perf-test5-app755
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
