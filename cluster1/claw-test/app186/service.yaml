apiVersion: v1
kind: Service
metadata:
  name: perf-test5-app186-svc
  namespace: clawtest
  labels:
    app: perf-test5-app186
spec:
  selector:
    app: perf-test5-app186
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
